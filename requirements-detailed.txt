# GPU加速版本依赖 (可选)
# 如果您有NVIDIA GPU并想使用GPU加速，请使用以下命令安装：
# pip install paddlepaddle-gpu
# 或者安装CPU版本：
# pip install paddlepaddle

# 基础依赖
paddleocr==2.7.3
paddlepaddle==2.5.2

# OpenVINO 支持
openvino==2023.1.0

# 图像处理
opencv-python==4.8.1.78
Pillow==10.0.1
numpy==1.24.3

# 命令行和进度条
tqdm==4.66.1

# 系统工具
pathlib2==2.3.7
werkzeug>=3.1.4 # not directly required, pinned by Snyk to avoid a vulnerability
